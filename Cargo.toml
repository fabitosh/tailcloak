[package]
name = "tailcloak"
authors = ["fabitosh"]
version = "0.1.0"
edition = "2024"

[dependencies]
netdev = { version = "0.44.0", features = ["serde"] }
dirs = "6.0.0"
serde = { version = "1.0.228", features = ["derive"] }
toml = "1.1.2"

[dev-dependencies]
tempfile = "3"
